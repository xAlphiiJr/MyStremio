# Release artifacts

Upload `MyStremioSetup-v*_x64.exe` to GitHub Releases. The installer is self-contained.

`SHA256SUMS.txt` is included for checksum verification. Installer binaries are gitignored.
